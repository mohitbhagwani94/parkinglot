{
  "info": {
    "name": "Parking Lot API",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Pricing",
      "item": [
        { "name": "List Pricing", "request": { "method": "GET", "url": "{{baseUrl}}/api/pricing" } },
        { "name": "Create Pricing", "request": { "method": "POST", "url": "{{baseUrl}}/api/pricing" } },
        { "name": "Update Pricing", "request": { "method": "PUT", "url": "{{baseUrl}}/api/pricing/:id" } },
        { "name": "Delete Pricing", "request": { "method": "DELETE", "url": "{{baseUrl}}/api/pricing/:id" } }
      ]
    }
  ]
}
